# KeeleSepp CRM v1 — передача состояния работ

Актуально: 30.08.2026, Europe/Tallinn
Репозиторий: `zakutailopavel-cyber/keelesepp`
Рабочий интерфейс: `haldus.html`
Текущая ветка: `codex/v1-terms`

## Граница задачи

Работа продолжается над первой CRM. `crm-v2/` в этом проходе не изменялся. Live Classroom остаётся последним этапом и не входит в текущий приоритет. Тарифы скрыты, отдельная модель поставщиков не нужна; для языковой школы используется обычный блок расходов.

Главный принцип v1: не переписывать архитектуру одним большим изменением и не ломать действующую Firebase-логику. Новые административные операции должны использовать точные ID, защищённые серверные маршруты, обязательную причину и журнал изменений.

## Что было сделано ранее

- канонические связи ученика и родителя через Firebase Auth UID;
- безопасное объединение дублей с preview, переносом зависимостей и архивированием старых карточек;
- центр исправления данных для потерянных связей, групповых уроков, счетов, аккаунтов и неверных дат;
- расходы с чеками, исправлением и аннулированием без удаления истории;
- закрытие финансового месяца, экспорт и корректировки закрытого периода;
- финансовый и бухгалтерский реестр, точные связи уроков, счетов и платежей;
- поиск, фильтры и выгрузки учеников, счетов и расходов;
- библиотека материалов и предпросмотр без скачивания;
- первичная диагностика ученика.

## Визуальный аудит 26.08.2026

Полный отчёт находится в `UX_AUDIT_V1_2026-08-26.md`. До изменений оценка составляла примерно 6,5/10 на компьютере и 4/10 для мобильной административной части.

Главные обнаруженные проблемы:

1. недельное расписание на 390 px сжималось в узкую полосу из-за минимальной ширины сетки и правой колонки;
2. бухгалтерский контроль показывал неоприходованные уроки, но не позволял принять решение в строке;
3. дубли в центре исправления трудно различать при одинаковом имени;
4. материал `Uus tööleht` имеет расхождение уровня A2 в карточке и B1 в содержимом;
5. списки учеников и финансовые экраны перегружены одновременно открытыми фильтрами и формами;
6. production всё ещё компилирует большой JSX через Babel и использует Tailwind CDN.

## Изменения текущего прохода

### Мобильное расписание

Изменены `haldus.html` и `haldus.css`:

- мобильный экран по умолчанию открывает режим `Päev`;
- при переходе desktop → mobile недельный режим безопасно переключается на дневной;
- добавлен недельный переключатель из семи дней;
- дневная сетка получает ширину экрана без горизонтального сжатия;
- правая сводка и `Õpilaste spikker` перемещаются под календарь;
- кнопки режима и навигации укладываются в мобильную сетку;
- пользователь всё ещё может вручную открыть неделю или месяц.

### Действия для неоприходованных уроков

В `LessonPaymentRegisterPanel` добавлена колонка `Toimingud`:

- `Arve` создаёт счёт только для выбранного урока через `addInvoice(studentId, {lessonIds:[lessonId]})`;
- `Tasuline` назначает `late_cancel_billable` для пропуска;
- `Tasuta` назначает `free` для завершённого урока или `cancelled_on_time` для пропуска;
- `Kanna maha` назначает `written_off`;
- кнопки блокируются на время операции;
- счёт требует отдельного подтверждения, а изменение статуса — существующую обязательную причину;
- серверная модель, финансовый аудит и административная часть не обходятся и не дублируются.

Изменение передано через существующую цепочку `App → AccountingRegisterView → LessonPaymentRegisterPanel`.

### Тема проведённого урока больше не обязательна

- урок из календаря можно сохранить в дневник со статусом `Toimunud`, не заполняя тему;
- поле явно подписано как необязательное;
- если тема не введена, сохраняется пустая строка, а существующие списки и финансовые документы используют предусмотренное нейтральное название `Tund` / `Toimunud tund`;
- остальные данные урока, пакетный учёт и финансовая логика не изменены.

### 27.08.2026 — системное исправление учёта уроков

Учёт проведённых уроков больше не опирается на несколько независимых записей из браузера. Добавлен защищённый серверный маршрут `POST /lessons/journal`, который одной транзакцией:

- проверяет роль администратора или назначенного преподавателя;
- требует точный `studentId` существующей карточки и больше не создаёт новые индивидуальные записи с техническим `studentId: ext`;
- сохраняет дневниковую запись, статус календаря и старые счётчики ученика согласованно;
- использует постоянный ID из расписания, даты и ученика, поэтому повторное нажатие, повтор запроса или другая вкладка не создают дубль;
- записывает отдельные статусы `Toimunud`, `Puudus_p` и `Puudus_eta`;
- для одного проведённого повторяющегося занятия меняет только выбранную дату, не всю будущую серию;
- создаёт для проведённого урока финансовый статус `unbilled`, чтобы запись сразу попадала в очередь выставления счёта;
- запускает идемпотентный пакетный учёт и явно ставит `needs_attention`, если движение пакета не удалось завершить.

Удаление дневниковой записи перенесено в защищённый маршрут `POST /lessons/journal/delete`. Он проверяет права, закрытый финансовый период, связь со счётом и пакетом, а затем одной транзакцией удаляет запись, восстанавливает календарь и корректирует старый счётчик. Удаление серии календаря блокируется, если от неё зависят несколько дневниковых записей.

### 27.08.2026 — рабочие действия в календаре

- у запланированного занятия за сегодня или прошедшую дату есть быстрое действие `Märgi toimunuks`;
- в боковой сводке появился список `Tänased lõpetamata tunnid`;
- тема урока при быстром завершении остаётся пустой и в списках отображается нейтральное название `Tund`;
- пропуски больше не возвращаются в состояние `Planeeritud`;
- групповой урок создаёт отдельную запись для каждого отмеченного ученика, поэтому его уроки и финансы видны в личной карточке;
- `Tuli`, `Hoiatas` и `Puudus` преобразуются соответственно в `Toimunud`, `Puudus_p` и `Puudus_eta`;
- файлы группового урока загружаются один раз и связываются со всеми созданными записями.

### 27.08.2026 — связь уроков с финансами

- новые проведённые уроки сразу имеют явный статус `unbilled`;
- в реестре до создания счёта отображается ожидаемая цена ученика и пометка `eeldatav`;
- отсутствие цены видно как `hind puudub`, а не как нулевая сумма;
- CSV реестра содержит ожидаемую сумму;
- календарная статистика отдельно считает пропуски и не включает отмены/пропуски в планируемый доход;
- подпись показателя уточнена до `Planeeritud tulu`.

## Проверка

- CRM v1: `node --test *.test.js` — 153/153 успешно;
- backend: `cd functions && npm test` — 103/103 успешно;
- итого: 256/256 локальных тестов успешно;
- `node --check functions/index.js` — успешно;
- `git diff --check` — успешно;
- добавлен тест мобильного календаря в `calendar-overlap-ui.test.js`;
- добавлен тест прямых финансовых решений в `finance-admin-completion.test.js`;
- добавлены модульные тесты стабильного ID и переходов статуса в `functions/lesson-record-core.test.js`;
- добавлены UI-проверки защищённой записи, быстрого завершения, групповой раскладки, ожидаемой цены и безопасного удаления в `lesson-accounting-ui.test.js`;
- в emulator integration добавлен полный сценарий создание → повтор → исправление на пропуск → удаление;
- GitHub Actions с Java 21 успешно выполнил Firebase Emulator integration: [Financial Core emulator, run 33046117853](https://github.com/zakutailopavel-cyber/keelesepp/actions/runs/33046117853);
- локальная CRM загружена при viewport 390×844 до экрана входа;
- ошибок JSX/React при загрузке нет;
- в консоли остаются только прежние предупреждения Tailwind CDN, Babel in-browser и inline script >500 KB.

### Production-публикация 27.08.2026

- релизный коммит `8354e7e` отправлен в `main`;
- Vercel deployment `dpl_CYkxmNWDQYUVVfy6jJfPfud96FtF` получил состояние `READY` и основной домен отдаёт `KeeleSepp CRM · 27.08.2026.1`;
- Firebase Function `financeApi` обновлена в `us-central1`, состояние `ACTIVE`, hash `c49e5e405fe1716d687116ae7df32dd48ad29f91`;
- production URL: `https://www.epkoolitus.ee/haldus/`;
- проверка выполнена в существующей административной сессии: главная панель и календарь загрузились, видны быстрые действия, `Puudumised`, `Planeeritud tulu` и `Tänased lõpetamata tunnid`;
- smoke-test был read-only: реальные уроки, счета, пакеты и карточки учеников не изменялись;
- Vercel runtime error scan за час после публикации не нашёл ошибок;
- защищённый production endpoint отвечает `401 Firebase ID token required` на запрос без авторизации, то есть анонимный доступ не открыт.

### Production-публикация 27.08.2026 — стабилизация календаря

Релиз `KeeleSepp CRM · 27.08.2026.2` закрывает следующий этап рабочего календаря:

- отменённые уроки больше не попадают в число пересечений;
- пересечения собираются в отдельную очередь `Ajaplaani konfliktid` без дублирования одной пары;
- в каждой строке видны дата, время, оба занятия и причина конфликта;
- `Näita →` переводит календарь в нужный день и визуально выделяет оба конфликтующих урока;
- счётчик в заголовке календаря также ведёт к первому конфликту;
- технические ответы Google Calendar (`invalid_request`, истёкшее разрешение, лимит запросов и другие) заменены понятным описанием следующего действия;
- для истёкшего или недостаточного разрешения появляется `Ühenda uuesti`;
- для повторяемой ошибки синхронизации появляется `Proovi uuesti`;
- ручной `POST /gcal/sync` теперь принудительно повторяет записи с прежним ошибочным fingerprint. Ранее `backfillScheduleToGoogle` пропускал тот же fingerprint после первой ошибки, поэтому кнопка `Sünkrooni` фактически не делала повторную попытку;
- автоматическая почасовая синхронизация оставлена без `force`, чтобы не создавать бесконечный цикл ошибочных запросов.

Проверка релиза:

- релизный коммит `6d4f15e` отправлен в `main`;
- Vercel deployment `dpl_6spuCDCVSFU6rEG2pVBGCH6Yh3oT` получил состояние `READY`;
- Firebase Function `gcalApi` обновлена в `us-central1`, состояние `ACTIVE`, hash `56764ecbc033406c1d4cc2715961700eae7a2f31`;
- GitHub Actions `Financial Core emulator`, run `33048513610`, завершён успешно;
- production URL: `https://www.epkoolitus.ee/haldus/`;
- в административной сессии подтверждена версия `27.08.2026.2`, очередь из 9 видимых конфликтов и переход к паре `Aleksandr Smirnov ↔ Mihhail Smirnov` в дневном режиме;
- для существующих ошибочных записей Милана вместо `invalid_request` видна инструкция проверить ученика, дату и время, затем повторить синхронизацию;
- Google Calendar остаётся подключённым, доступны `Sünkrooni` и `Seaded`;
- Vercel runtime error scan за час после публикации не нашёл ошибок;
- production smoke-test был read-only: синхронизация, быстрое завершение, изменение и удаление реальных уроков не запускались.

После публикации для первого рабочего использования рекомендуется:

1. открыть `Tunniplaan` на телефоне;
2. проверить выбор дня, предыдущий/следующий день, создание и открытие урока;
3. открыть `Finants → Raamatupidamine → Tunnid ↔ maksed`;
4. провести один обычный и один повторяющийся урок без темы, затем повторно нажать сохранение и убедиться, что дубль не появился;
5. проверить отдельные статусы двух видов пропуска;
6. провести групповой урок с разными отметками учеников и проверить их личные карточки;
7. на тестовом неоприходованном уроке проверить каждое финансовое решение;
8. убедиться, что после создания счёта урок исчезает из очереди и появляется точная строка счёта;
9. проверить, что месячный блокирующий счётчик уменьшается;
10. проверить удаление свободного пропуска и блокировку удаления урока со счётом/пакетом.

## Следующий порядок работ

1. На двух известных ошибочных занятиях Милана проверить правильность studentId, даты и времени, затем один раз нажать `Sünkrooni`; новая серверная логика выполнит реальный повтор.
2. По очереди `Ajaplaani konfliktid` принять решение по 9 текущим пересечениям; групповые уроки не исправлять автоматически, так как одновременные группы могут быть сознательной записью или ошибкой преподавателя.
3. Наблюдать первые реальные сохранения обычного, повторяющегося и группового урока и при необходимости проверить `needs_attention` у пакетного движения.
4. Улучшить preview дублей: email, учитель, родитель, количество уроков/счетов, последние символы ID, поиск вместо длинного select.
5. Добавить проверку согласованности уровня папки/карточки и содержимого материала до публикации.
6. Свернуть дополнительные фильтры учеников в `Rohkem filtreid`, добавить активные чипы и сброс.
7. Упростить таблицу базы: основные закреплённые столбцы, выбор столбцов и раскрываемые детали.
8. Разделить `Arved` на обзор, создание и список; выделить очередь просроченных/неотправленных счетов.
9. Компактно перестроить группы и родителей.
10. Улучшить доступность и затем убрать production-компиляцию Babel/Tailwind CDN.

## Не трогать без отдельного решения

- Live Classroom;
- тарифный интерфейс;
- несуществующий для школы справочник поставщиков;
- автоматическое объединение карточек только по похожему имени или email;
- удаление Auth-пользователей и исторических финансовых записей;
- `crm-v2/` и находящиеся там несвязанные пользовательские файлы.

## Локальное состояние файлов

Файлы текущего изменения:

- `haldus.html`;
- `haldus.css`;
- `haldus-shared.js`;
- `calendar-core.js`;
- `calendar-core.test.js`;
- `calendar-overlap-ui.test.js`;
- `finance-admin-completion.test.js`;
- `lesson-accounting-ui.test.js`;
- `functions/index.js`;
- `functions/lesson-record-core.js`;
- `functions/lesson-record-core.test.js`;
- `functions/finance-emulator.integration.js`;
- `UX_AUDIT_V1_2026-08-26.md`;
- `HANDOFF_V1_2026-08-26.md`.

В `crm-v2/` есть несвязанные untracked-файлы пользователя с суффиксом ` 2`; их нельзя добавлять, изменять или удалять в рамках этой работы.

## Production hotfix 27.08.2026: сохранение дневника после объединения ученика

Симптом в production:

- при сохранении дневника для `Milan Grozovski`, 27.08.2026 в 13:00, интерфейс показывал общее сообщение `Päeviku salvestamine ei õnnestunud. Proovi uuesti või eemalda failid.`;
- запрос `POST /lessons/record` завершался с HTTP 409;
- точный ответ сервера: `Schedule entry belongs to another student`.

Точная причина:

- после объединения дублей Милана архивные карточки остались в подписке `students`, что правильно для сохранения истории;
- календарная запись уже ссылалась на основную карточку ученика;
- форма дневника выбирала ученика только через первое совпадение имени;
- при одинаковых именах могла быть выбрана архивная карточка, и в запрос попадал другой `studentId`;
- серверная проверка связи календаря и ученика правильно блокировала такую запись. Проверка не ослаблялась.

Исправление:

- в `haldus-shared.js` добавлен единый `resolveStudentRecord(students, record)`;
- сначала используется точный `studentId` календарной записи;
- ссылка `mergedIntoStudentId` приводит архивную карточку к основной;
- совпадение по имени допускается только для одной активной канонической карточки, неоднозначное имя не угадывается;
- эта логика подключена к полной форме дневника и к быстрому завершению урока;
- вместо общей подсказки удалить файлы интерфейс теперь показывает конкретное действие для ошибок связи ученика, финансового периода, выставленного счёта, прав преподавателя и размера вложений;
- `financeApi` пишет безопасный структурированный warning с путём, статусом и ограниченным текстом ошибки без тела запроса и персональных данных;
- версия интерфейса обновлена до `KeeleSepp CRM · 27.08.2026.3`.

Изменённые файлы:

- `haldus.html`;
- `haldus-shared.js`;
- `functions/index.js`;
- `student-record-resolution.test.js`;
- `lesson-accounting-ui.test.js`;
- `student-export-ui.test.js`.

Проверка и публикация:

- 158 тестов корневого проекта прошли;
- 103 теста Firebase Functions прошли;
- всего 261/261 тестов;
- `node --check functions/index.js` и `git diff --check` прошли;
- кодовый коммит `4b426db` отправлен в `main`;
- Vercel deployment `dpl_FmiXE3QtgfaPeT2Cqw84jGxEvsTi` получил состояние `READY`;
- Firebase Function `financeApi` успешно обновлена в `us-central1`;
- GitHub Actions `Financial Core emulator`, run `33111838836`, завершён успешно;
- в новой административной вкладке production подтверждена версия `27.08.2026.3`;
- открыта точная форма `Milan Grozovski · 27.08.2026 · 13:00`, загрузка формы прошла без прикладных ошибок;
- единственная запись уровня error в консоли — известное предупреждение Babel о размере inline-скрипта;
- реальное сохранение урока не выполнялось, чтобы не изменять учебные данные без участия пользователя.

Пользователю нужно обновить старую вкладку, где ещё отображается версия `27.08.2026.1`, затем снова открыть урок Милана и сохранить дневник. Перед повтором проверить внизу меню версию `27.08.2026.3`.

## Release 29.08.2026: õppekavad в CRM v1 и на публичном сайте

Цель релиза:

- проверить переданный интерактивный прототип языковых программ;
- сохранить подробные методические сценарии внутри авторизованной CRM;
- вынести на публичный сайт только полезную для клиента обзорную часть;
- не менять Firebase-схему, реальные учебные данные и `crm-v2/`.

Проверенный объём исходного материала:

- 11 уровневых программ: эстонский A1–C1 и английский A1–C2;
- 79 тематических блоков;
- 158 готовых сценариев урока, по два на каждую тему;
- 632 словарные единицы;
- у каждого урока сохранены цель, пятиэтапная структура, материалы, оценивание и возрастная адаптация.

Архитектурное решение:

- данные извлекаются воспроизводимым скриптом `scripts/extract-curriculum.py`;
- результат хранится в статическом модуле `haldus-curriculum-data.js` и не требует миграции Firestore;
- полный методический контент доступен администраторам и преподавателям через пункт `Õppekavad` в CRM v1;
- публичная страница `/oppekavad/` показывает уровни, объём, методику, связь со школьной программой и подготовкой к экзаменам без публикации внутренних сценариев;
- формулировки на публичной странице не создают впечатление, что KeeleSepp проводит государственные экзамены;
- исходный внутренний методический текст пока преимущественно на русском языке, интерфейс CRM — на эстонском, публичная страница — на эстонском и английском.

Что добавлено в CRM v1:

- метрики по программам;
- фильтр по языку и уровню;
- поиск по темам, словарю и целям уроков;
- раскрываемые уровни и тематические карточки;
- грамматика, навыки, школьная и экзаменационная привязка;
- два полных сценария внутри каждой темы;
- словарные ссылки;
- `Eelvaade`, который показывает цели, активный словарь и структуру урока в модальном окне без скачивания файла;
- ссылка из CRM на публичное представление программ.

Что добавлено на публичный сайт:

- отдельная SEO-страница `https://www.epkoolitus.ee/oppekavad/`;
- краткий обзор всех поддерживаемых уровней;
- понятное описание методики и объёма;
- ссылки на официальный HARNO и действующую государственную программу Riigi Teataja;
- переходы к tasemetest и регистрации;
- ссылки в главном меню, первом экране, отдельном обзорном блоке и footer;
- маршрутизация Vercel, sitemap и закрытие административных путей в robots.txt.

Изменённые и новые файлы:

- `scripts/extract-curriculum.py`;
- `haldus-curriculum-data.js`;
- `haldus.html`;
- `haldus.css`;
- `haldus-shared.js`;
- `curriculum-ui.test.js`;
- `haldus-curriculum-data.test.js`;
- `student-export-ui.test.js`;
- `oppekavad.html`;
- `index.html`;
- `vercel.json`;
- `sitemap.xml`;
- `robots.txt`.

Проверка:

- генератор повторно создал байт-в-байт тот же `haldus-curriculum-data.js`;
- 164/164 теста корневого проекта прошли;
- 103/103 теста Firebase Functions прошли;
- всего 267/267 тестов;
- `git diff --check` прошёл;
- публичная страница проверена визуально на desktop и mobile 390×844;
- переключение языка публичной страницы проверено;
- production CRM проверена в авторизованной административной сессии;
- подтверждена версия `KeeleSepp CRM · 29.08.2026.2`;
- подтверждены пункт меню `Õppekavad`, числа 11 / 79 / 158 / 632, фильтры и поиск;
- открыт уровень Eesti keel A1 и предпросмотр темы `Знакомство и приветствие`;
- в предпросмотре подтверждены цели двух уроков, восемь слов, пятиэтапная структура и сообщение, что скачивание не выполняется.

Публикация:

- основной релизный коммит: `dd39adb` (`feat: add curriculum workspace and public guide`);
- исправление подключения экрана к рабочему меню: `957ec77` (`fix: expose curriculum workspace in CRM`);
- production deployment: `dpl_5RgbHds2Lrdvv3QhfU1L9i1WnoKo`, состояние `READY`;
- GitHub Actions `Financial Core emulator`, run `33215322219`, завершён успешно;
- Firebase Functions не менялись и не публиковались;
- миграция базы данных не требовалась;
- пользовательские несвязанные файлы в `crm-v2/` не изменялись.

Возможные следующие улучшения, не входящие в этот релиз:

1. Перевести полный внутренний методический контент с русского на эстонский или сделать двуязычную редакцию.
2. Связать темы программы с прогрессом конкретного ученика и записями дневника.
3. Добавить контролируемое создание учебного материала из выбранной темы в существующую библиотеку.
4. Добавить версионирование и редакционное утверждение программ, если методисты начнут менять их непосредственно в CRM.
5. Не добавлять сертификаты или официальные утверждения без отдельной проверенной процедуры выдачи и юридического основания.

## Release 29.08.2026.3: õppekava работает только внутри CRM v1

Эта секция заменяет решение предыдущего релиза о публичной странице программ. По прямому решению владельца проекта учебные программы больше не публикуются на публичном сайте. Методические сценарии и связанные с ними рабочие действия доступны только в CRM v1 для администратора и преподавателей.

Что удалено с публичного сайта:

- страница `oppekavad.html`;
- пункты `Õppekavad` в главном меню и footer;
- кнопка на первом экране и обзорный блок 11 / 79 / 158 / 632;
- маршруты и заголовки `/oppekavad` и `/oppekavad/` в `vercel.json`;
- адрес `/oppekavad/` из `sitemap.xml`;
- ссылка `Avalik vaade` из внутреннего раздела CRM.

После публикации `https://www.epkoolitus.ee/oppekavad/` возвращает HTTP 404, на главной странице нет ссылок на этот адрес, а sitemap содержит только главную страницу, tasemetest, privaatsus и tingimused. Статический модуль программы остаётся техническим ресурсом CRM v1, потому что старый интерфейс загружает данные непосредственно в браузере.

Что добавлено только в CRM v1:

- кнопка `Kasuta õppetöös` у каждой темы программы;
- выбор конкретного ученика и одного из двух сценариев темы;
- `Määra õpiteemaks`: сохраняет в карточке ученика текущую тему, цель и точный источник программы;
- `Lisa õppevarasse`: создаёт готовый материал в существующей коллекции `curriculumLessons` и не создаёт повторный материал с тем же `sourceKey`;
- `Saada kodutöö`: создаёт домашнее задание для ученика и блокирует повторную ожидающую работу из того же сценария;
- ссылка после создания материала открывает правильную папку предмета, уровня и темы в `haldus-exercises`;
- в карточке ученика появился блок `Õppekava progress` с количеством пройденных сценариев и тем, процентом, назначенной темой и следующим рекомендуемым сценарием;
- прогресс вычисляется из уже существующих проведённых уроков, связанных с программой, поэтому миграция старых записей не требуется;
- в форме дневника появился селектор `Õppekava teema`; он показывает только предмет и CEFR-уровень выбранного ученика и заполняет тему, цель, словарь и рекомендацию на следующий раз;
- сервер дневника сохраняет и проверяет `curriculumLanguageId`, `curriculumSubject`, `curriculumLevel`, `curriculumTopicId`, `curriculumTopicName`, `curriculumLessonIndex` и `curriculumLessonGoal`;
- назначение темы и домашнего задания блокируется, если предмет или уровень ученика не совпадает с выбранной программой;
- библиотека блокирует сохранение материала при явном конфликте уровня папки, метаданных программы или обозначения уровня внутри содержимого; предупреждения без конфликта требуют отдельного подтверждения.

Новая общая логика:

- файл `curriculum-workflow-core.js` содержит построение каталога из 158 сценариев, создание записей темы/материала/домашнего задания, строгую проверку ученика и материала, а также расчёт прогресса;
- файл `curriculum-workflow-core.test.js` проверяет стабильность идентификаторов, сохранение принадлежности к программе, блокировку несовпадающего уровня и расчёт прогресса без миграции;
- реальные учебные записи во время production-проверки не создавались и не изменялись.

Изменённые и новые файлы релиза:

- `curriculum-workflow-core.js`;
- `curriculum-workflow-core.test.js`;
- `curriculum-ui.test.js`;
- `haldus.html`;
- `haldus.css`;
- `haldus-exercises/index.html`;
- `haldus-shared.js`;
- `student-export-ui.test.js`;
- `functions/index.js`;
- `index.html`;
- `vercel.json`;
- `sitemap.xml`;
- удалён `oppekavad.html`.

Проверка релиза:

- `node --test *.test.js`: 169/169 тестов CRM v1 прошли;
- `npm test` в `functions/`: 103/103 серверных тестов прошли;
- всего 272/272 относящихся к v1 тестов;
- `node --check curriculum-workflow-core.js`, `node --check functions/index.js` и `git diff --check` прошли;
- GitHub Actions `Financial Core emulator`, run `33235490062`, завершён успешно для commit `59f3591`;
- Firebase Function `financeApi` успешно обновлена в `us-central1`;
- Vercel deployment `dpl_J8sYWYk8NDx7RsiGZU8kkAGgP4Ye` получил состояние `READY` и production aliases;
- Vercel runtime error scan за один час не нашёл ошибок;
- в production под администратором подтверждена версия `KeeleSepp CRM · 29.08.2026.3`;
- подтверждены 11 уровней, 79 тем, 158 сценариев и 632 словарные единицы;
- открыта тема Eesti keel A1 `Знакомство и приветствие` и окно `Kasuta õppetöös`;
- для ученика A1 кнопки назначения темы и домашней работы активны, а после выбора ученика A2 они блокируются с сообщением `Õpilase tase A2 ei vasta õppekava tasemele A1.`;
- в карточке Agata подтверждён блок `Õppekava progress`: 0/16 сценариев, 0/8 тем, следующий сценарий `Знакомство и приветствие · Урок 1`;
- в дневнике Agata подтверждён список только из 16 сценариев Eesti keel A1;
- мобильное окно действий проверено: кнопки перестраиваются в одну колонку и не выходят за ширину экрана;
- единственное сообщение уровня error в консоли — прежнее уведомление Babel о деоптимизации генерации для inline-скрипта больше 500 KB; прикладных ошибок нового функционала не обнаружено.

Коммит кода релиза: `59f3591` (`feat: connect curriculum workflows in CRM`). Несвязанные пользовательские файлы в `crm-v2/` не добавлялись и не изменялись.

## Release 29.08.2026.4: õppekava → tööleht единым процессом

Раздел учебных программ и конструктор рабочих листов теперь связаны в один рабочий сценарий CRM v1. Публичный сайт, CRM v2 и Live Classroom этим релизом не менялись.

Что изменилось для преподавателя:

- в окне `Kasuta õppetöös` появилась команда `Loo tööleht`;
- выбранный язык, CEFR-уровень, тема и один из двух сценариев передаются непосредственно в конструктор;
- конструктор открывается не пустым: создаются блок цели урока, сопоставления активного словаря, словарной таблицы и самостоятельного письменного задания;
- объём словаря и письменного задания меняется в зависимости от уровня;
- источник программы передаётся в AI-панель как контекст вместе с целью, этапами, словарём, материалами и оцениванием;
- кнопка `Täienda AI-ga` открывает уже заполненную AI-панель, поэтому преподавателю не нужно повторно копировать тему и сценарий;
- добавлен видимый контроль качества из пяти пунктов: заголовок, предмет/уровень/тема, структура, активное задание и проверяемый ответ;
- конструктор адаптирован для телефона: при 390 px панели идут друг под другом, лист занимает доступную ширину и горизонтального переполнения нет.

Исправления качества и данных:

- AI больше не использует эстонский методический профиль для английского материала;
- добавлены отдельные профили English CEFR A1–C2 и Eesti CEFR C2;
- все ученические тексты генерируются на выбранном языке, а не всегда на эстонском;
- EKI-словарь доступен только для предмета `Eesti keel` и не смешивается с английскими листами;
- при сохранении сохраняются `sourceKey` и все поля принадлежности к программе: язык, предмет, уровень, topic id/name, индекс и цель сценария, версия источника;
- если материал с тем же `sourceKey` уже создан, конструктор предлагает обновить именно его вместо создания дубля;
- выбранные в окне сохранения предмет, уровень и тема синхронизируются с `worksheetData.meta`;
- качество и источник сохраняются также в `worksheetAssignments`, чтобы назначенный ученику лист не терял связь с программой;
- существующие учебные данные не мигрировались и не удалялись.

Новая общая логика:

- `curriculum-workflow-core.js::buildWorksheetPrefill` создаёт детерминированную редактируемую основу листа;
- `curriculum-workflow-core.js::analyzeWorksheet` выполняет общий пятиступенчатый контроль качества;
- `haldus-worksheet/index.html` принимает одноразовый `ws_prefill`, заполняет обычный и AI-режимы, валидирует уровень при сохранении и сохраняет curriculum ownership;
- версия CRM поднята до `KeeleSepp CRM · 29.08.2026.4`.

Изменённые и новые файлы:

- `curriculum-workflow-core.js`;
- `curriculum-workflow-core.test.js`;
- `curriculum-ui.test.js`;
- `worksheet-builder-ui.test.js`;
- `haldus.html`;
- `haldus.css`;
- `haldus-worksheet/index.html`;
- `haldus-shared.js`;
- `student-export-ui.test.js`.

Проверка релиза:

- `node --test *.test.js`: 174/174 теста CRM v1 прошли;
- `npm test --prefix functions`: 103/103 серверных теста прошли;
- всего 277/277 тестов;
- `git diff --check` прошёл;
- локальный конструктор загружен реальным браузером без JavaScript errors;
- на ширине 390 px подтверждены: `workspaceDisplay=flex`, `workspaceDirection=column`, ширина панели 390 px, ширина A4 preview 374 px, `bodyScrollWidth=390`;
- production отдаёт версию `KeeleSepp CRM · 29.08.2026.4`, кнопку `Loo tööleht`, вызов `buildWorksheetPrefill`, модуль `curriculum-workflow-core.js`, English CEFR profiles и карточку `Töölehe kvaliteet`;
- production-конструктор загрузился без console errors;
- Vercel deployment `dpl_2H7dYKsArMBGgvaqUFNvAVMJx5EK` имеет состояние `READY`, target `production`, commit `ece0623`;
- Vercel runtime error scan за последний час не нашёл ошибок;
- GitHub Actions `Financial Core emulator`, run `33254910907`, завершён успешно;
- Firebase Functions и правила не менялись, публикация backend не требовалась.

Коммит кода релиза: `ece0623` (`feat: connect curricula to worksheet builder`). Несвязанные пользовательские файлы в `crm-v2/` не добавлялись и не изменялись.

Следующий логичный этап этого направления:

1. Добавить библиотеку утверждённых шаблонов рабочих листов по типу урока и возрасту, сохраняя текущий свободный конструктор.
2. Добавить версионирование листа и статусы `mustand / kontrollitud / avaldatud`, чтобы изменения не перезаписывали уже выданную ученику версию.
3. Добавить экран результатов назначенных листов: ответы, автоматическая проверка закрытых заданий, комментарий преподавателя и связь результата с навыком/темой программы.
4. Вынести тяжёлый inline Babel-код конструктора в собираемые модули; это отдельная техническая модернизация v1 и не должна смешиваться с CRM v2.

## Release 29.08.2026.5: безопасный конструктор рабочих листов

Конструктор рабочих листов CRM v1 получил самостоятельный workflow создания, редактирования, проверки и публикации. Публичный сайт, CRM v2 и Live Classroom не менялись.

Что изменилось для преподавателя:

- добавлена вкладка `Mallid` с пятью редактируемыми методическими структурами: словарь, грамматика, чтение, повторение и коммуникация;
- шаблоны учитывают предмет, CEFR-уровень и язык задания;
- незавершённый лист автоматически сохраняется в браузере и восстанавливается после перезагрузки или случайного закрытия вкладки;
- сохранённый лист из библиотеки теперь открывается в конструкторе с правильными `lessonId`, метаданными и блоками;
- кнопка редактирования сохраняет новую версию вместо прямой перезаписи существующего листа;
- в окне сохранения появились состояния `Mustand`, `Kontrollitud`, `Avaldatud`;
- публикация блокируется, пока лист не пройдёт все пять пунктов контроля качества;
- ученику можно назначить только опубликованную версию;
- номер опубликованной версии отображается в выборе листа и сохраняется в назначении ученику.

Защита существующих данных:

- каждая запись создаётся в append-only коллекции `worksheetVersions`;
- правила Firestore разрешают сотруднику читать и создавать версии, но запрещают изменение и удаление истории;
- `worksheetAssignments` по-прежнему получает полный snapshot опубликованного листа, поэтому дальнейшее редактирование оригинала не меняет уже выданное задание;
- новый черновик не заменяет предыдущую опубликованную копию;
- при первом редактировании старого листа без версии его текущее содержимое автоматически фиксируется как опубликованная версия 1;
- старые листы без новых служебных полей продолжают считаться опубликованными, миграция или удаление данных не требуются.

Новая общая логика:

- `worksheet-workflow-core.js` хранит статусы, каталог шаблонов, правила назначения и построение version snapshots;
- `haldus-worksheet/index.html` отвечает за автосохранение, шаблоны, quality gate и транзакционное сохранение версий;
- `haldus-exercises/index.html` передаёт выбранный материал в конструктор;
- `haldus.html` выдаёт ученику только опубликованный snapshot;
- версия CRM: `KeeleSepp CRM · 29.08.2026.5`.

Проверка релиза:

- `node --test *.test.js`: 183/183 теста CRM v1 прошли;
- `npm test --prefix functions`: 103/103 серверных теста прошли;
- всего 286/286 тестов;
- `git diff --check` прошёл;
- браузером подтверждены пять шаблонов, создание трёхблочного листа, восстановление тех же трёх блоков после перезагрузки и отсутствие console errors;
- из локальной библиотеки выбранный лист `Igapäevased tegevused` открылся в конструкторе как `preview-sheet` с метаданными Eesti keel / A2 / Igapäevaelu и кнопкой `Salvesta uus versioon`;
- мобильная проверка 390x844: ширина документа 374 px, `bodyScrollWidth=390`, горизонтального переполнения нет;
- production deployment `dpl_43PuH88rnhmsDChgtdrFFarQvuys` имеет состояние `Ready`, target `production`;
- production-конструктор показывает вкладку `Mallid`, пять шаблонов и не имеет console errors;
- Vercel error logs за 15 минут пусты;
- Firestore rules скомпилированы и опубликованы в проект `keelesepp-5136b`;
- GitHub Actions `Financial Core emulator`, run `33255702207`, завершён успешно.

Коммит кода релиза: `755fc8b` (`feat: version and safeguard worksheet workflow`). Несвязанные пользовательские файлы в `crm-v2/` не добавлялись и не изменялись.

Следующий логичный этап конструктора:

1. Добавить экран истории версий с просмотром различий и восстановлением старой версии как новой, не изменяя историю.
2. Добавить результаты назначенных листов: фильтры, ручной комментарий преподавателя, автоматическую проверку закрытых заданий и связь результата с навыком программы.
3. Добавить собственные школьные шаблоны: сохранение удачного листа как утверждённого шаблона с возрастной группой и автором.

## Release 29.08.2026.6: схемы, комиксы и быстрые действия с блоками

Конструктор рабочих листов CRM v1 получил два полноценных визуальных типа и более быстрый монтаж длинных материалов. Публичный сайт, CRM v2 и Live Classroom не менялись.

Что изменилось для преподавателя:

- добавлен блок `Skeem` с тремя представлениями: последовательность шагов, цикл и мыслительная карта;
- у схемы редактируются заголовок, центральное понятие, количество частей, тексты и цвет каждой части;
- добавлен блок `Koomiks` с несколькими кадрами, персонажами, эмоциями, сценами, репликами и подписями;
- в каждый кадр комикса можно загрузить отдельную иллюстрацию; без файла используется наглядный персонаж-эмодзи;
- схема и комикс одинаково отображаются в конструкторе, библиотечном preview, печати/PDF и личном кабинете ученика;
- AI-генератор и точечная AI-перегенерация понимают структуры `diagram` и `comic`;
- каждый блок можно дублировать одной кнопкой;
- новый блок можно вставить непосредственно после выбранного, не перетаскивая его с конца списка;
- добавлены `Võta tagasi` и `Tee uuesti` с историей до 40 изменений;
- добавлена команда `Sulge`, которая сворачивает все редакторы блоков;
- старые перемещение мышью, стрелки порядка, удаление и изменение ширины сохранены.

Совместимость и данные:

- новые типы сохраняются внутри существующего worksheet snapshot и не требуют миграции Firestore;
- старые рабочие листы продолжают открываться без изменений;
- визуальные блоки являются информационными и не влияют на автоматический балл закрытых заданий;
- версия CRM: `KeeleSepp CRM · 29.08.2026.6`.

Изменённые файлы:

- `haldus-worksheet/index.html`;
- `haldus-exercises/index.html`;
- `haldus.html`;
- `haldus-shared.js`;
- `worksheet-builder-ui.test.js`;
- `worksheet-integration-ui.test.js`;
- `student-export-ui.test.js`.

Проверка релиза:

- `node --test *.test.js`: 185/185 тестов CRM v1 прошли;
- `npm test --prefix functions`: 103/103 серверных теста прошли;
- всего 288/288 тестов;
- `git diff --check` прошёл;
- реальным браузером подтверждены добавление схемы и комикса, редакторы обоих типов и их preview;
- подтверждены дублирование блока и цепочка 6 блоков → отмена до 5 → повтор до 6;
- подтверждена вставка нового блока после выбранного, а не только в конец листа;
- мобильная проверка 390x844: workspace переключается в `flex`, визуальные сетки становятся одноколоночными, горизонтального переполнения нет;
- production deployment `dpl_GrU3KVta7ahP3RZKMQ614uR4UCpS` имеет состояние `Ready`, target `production`;
- production отдаёт `Skeem`, `Koomiks`, undo/redo, duplicate/insert и версию `29.08.2026.6` без browser console errors;
- Vercel runtime error logs пусты;
- GitHub Actions `Financial Core emulator`, run `33256407394`, завершён успешно;
- Firebase Functions и Firestore rules не менялись, отдельная публикация backend не требовалась.

Коммит кода релиза: `ec95ee1` (`feat: add visual worksheet blocks and editing controls`). Несвязанные пользовательские файлы в `crm-v2/` не добавлялись и не изменялись.

Следующий логичный этап конструктора:

1. Добавить библиотеку готовых школьных схем и сюжетов комиксов, чтобы преподаватель вставлял их одним кликом и затем редактировал.
2. Добавить выбор нескольких блоков и групповые действия: перемещение, дублирование и удаление выбранных блоков.
3. Добавить историю версий с визуальным сравнением и восстановлением старой версии как новой.

## Release 29.08.2026.7: интерактивные визуальные задания

Визуальные блоки конструктора CRM v1 стали полноценными заданиями с ответами ученика и автоматической проверкой. Публичный сайт, CRM v2 и Live Classroom не менялись.

Что изменилось для преподавателя:

- добавлен блок `Ühenda nooltega`: преподаватель задаёт пары, ученик выбирает элементы слева и справа, а интерфейс рисует между ними стрелки;
- добавлен блок `Märgi pilt`: преподаватель загружает изображение, выбирает маркер и ставит его в нужное место кликом по картинке;
- у задания с изображением доступны точные координаты, правильные названия и включаемый словарь подсказок;
- в блоке `Skeem` можно отметить отдельные элементы как поля, которые должен заполнить ученик;
- в блоке `Koomiks` появились режимы чтения, дополнения выбранных реплик и восстановления правильной последовательности кадров;
- редактор позволяет выбрать, какие именно реплики ученик должен дописать;
- новые задания отображаются в рабочем листе, библиотечном preview, печати/PDF и кабинете ученика;
- AI-схема генерации понимает `connect`, `image_label`, интерактивные узлы схемы и режимы комикса;
- quality gate считает эти блоки активными заданиями с проверяемым ключом ответа.

Что изменилось для ученика:

- соединения выполняются кликами, выбранные пары сразу показываются стрелками;
- после проверки правильные стрелки становятся зелёными, ошибочные — красными;
- возле маркеров изображения есть поля для ответа и, при необходимости, перемешанный словарь;
- скрытые элементы схемы заполняются прямо внутри цепочки или карты;
- выбранные реплики комикса вводятся в кадрах, а кадры режима порядка перемещаются кнопками;
- все визуальные ответы сохраняются в существующем документе назначения и входят в общий балл;
- журнал ошибок показывает правильный и введённый ответ без раскрытия данных других заданий или учеников.

Архитектура и совместимость:

- добавлен общий модуль `worksheet-interactive-core.js` для одинаковой проверки ответов в интерфейсе и тестах;
- новые поля находятся внутри существующего worksheet snapshot, поэтому миграция Firestore не требуется;
- старые схемы и комиксы без интерактивных полей продолжают работать как информационные блоки;
- Firebase Functions и Firestore rules не менялись;
- версия CRM: `KeeleSepp CRM · 29.08.2026.7`.

Изменённые файлы:

- `haldus-worksheet/index.html`;
- `haldus-exercises/index.html`;
- `haldus.html`;
- `haldus-shared.js`;
- `curriculum-workflow-core.js`;
- `worksheet-interactive-core.js`;
- `curriculum-workflow-core.test.js`;
- `worksheet-interactive-core.test.js`;
- `worksheet-builder-ui.test.js`;
- `worksheet-integration-ui.test.js`;
- `student-export-ui.test.js`.

Проверка релиза:

- `node --test *.test.js`: 191/191 тест CRM v1 прошёл;
- `npm test --prefix functions`: 103/103 серверных теста прошли;
- всего 294/294 проверки;
- `git diff --check` прошёл;
- локальным браузером подтверждены четыре интерактивных режима, редакторы, печатный preview и переключение режимов комикса;
- мобильная проверка 390x844: ширина документа и viewport совпадают, горизонтального переполнения нет;
- локальная и production-проверки не обнаружили browser console errors или error overlay;
- production deployment `dpl_DMZDwMNoVcbuq3kpARvhkfmiRFbT` имеет состояние `Ready`, target `production`;
- production отдаёт новые блоки, общий scoring core и версию `29.08.2026.7`;
- Vercel runtime error logs за период публикации пусты;
- GitHub Actions `Financial Core emulator`, run `33257751157`, завершён успешно.

Коммит кода релиза: `bfccc52` (`feat: add interactive visual worksheet tasks`). Несвязанные пользовательские файлы в `crm-v2/` не добавлялись и не изменялись.

Следующий логичный этап конструктора:

1. Добавить библиотеку готовых изображений, схем и сюжетов комиксов с фильтрами по языку, уровню и возрасту.
2. Добавить визуальное сравнение версий рабочего листа и восстановление выбранной версии как новой.
3. Добавить преподавательский экран результатов: ответы по блокам, комментарий, повторное назначение ошибок и связь результата с навыками программы.

## Release 30.08.2026.8: визуальный банк рабочих листов

В CRM v1 добавлена библиотека готовых интерактивных визуальных заготовок. Публичный сайт, CRM v2 и Live Classroom не менялись.

Что появилось в конструкторе:

- вкладка `Mallid` разделена на полные шаблоны листа и новый `Visuaalne pank`;
- визуальный банк содержит 10 готовых редактируемых заданий;
- доступны фильтры по языку, уровню CEFR, возрасту и типу задания;
- добавлен текстовый поиск по названию, описанию и ключевым словам;
- заготовка добавляется в конец текущего листа и не заменяет уже созданные блоки;
- после вставки сразу открывается обычный редактор блока, поэтому любой текст, ответ, маркер, цвет или кадр можно изменить;
- список адаптируется под мобильную ширину и показывает количество найденных заготовок.

Состав банка:

- `Kehaosad pildil / Body parts on a picture`;
- `Esemed klassiruumis / Classroom objects`;
- `Minu päeva järjestus / My daily routine`;
- `Lause ehitamise skeem / Sentence builder`;
- `Teema mõistekaart / Topic mind map`;
- `Kohvikus tellimine / Ordering at a cafe`;
- `Abi küsimine koolis / Asking for help at school`;
- `Töövestlus / Job interview`;
- `Tunne ja sobiv vastus / Emotion and suitable response`;
- `Sõna ja seletus / Word and definition`.

Uued visuaalsed materjalid:

- KeeleSepp stiilis kehaosade illustratsioon;
- klassiruumi illustratsioon;
- kohviku dialoogi illustratsioon;
- töövestluse illustratsioon;
- kõik neli on lokaalsed SVG-failid, ei sõltu välisest pilditeenusest ja töötavad printimisel/PDF-is.

Архитектура и совместимость:

- каталог, фильтры и построение заготовок находятся в отдельном UMD-модуле `worksheet-visual-library.js`;
- каждый добавленный блок получает `visualPresetId`, сохраняя существующий формат worksheet block;
- миграция Firestore, новые права и серверные функции не требуются;
- старые рабочие листы и прежние шаблоны продолжают работать без изменений;
- версия: `KeeleSepp CRM · 30.08.2026.8`.

Изменённые и добавленные файлы:

- `haldus-worksheet/index.html`;
- `worksheet-visual-library.js`;
- `worksheet-visual-library.test.js`;
- `assets/worksheet-visuals/body-parts.svg`;
- `assets/worksheet-visuals/classroom.svg`;
- `assets/worksheet-visuals/cafe-scene.svg`;
- `assets/worksheet-visuals/interview-scene.svg`;
- `haldus.html`;
- `haldus-shared.js`;
- `worksheet-builder-ui.test.js`;
- `student-export-ui.test.js`.

Проверка релиза:

- `node --test *.test.js`: 197/197 тестов CRM v1 прошли;
- `npm test --prefix functions`: 103/103 серверных теста прошли;
- всего 300/300 проверок;
- `git diff --check` прошёл;
- все четыре SVG-файла прошли XML-валидацию;
- браузером проверены фильтры, поиск с одним результатом и добавление английского A1-задания по частям тела;
- существующие 11 блоков сохранились, новое задание добавилось 12-м;
- иллюстрация успешно загрузилась и отображалась в редакторе и preview;
- мобильная проверка 390x844: фильтры выстроены в одну колонку, ширина документа совпадает с viewport, горизонтального переполнения нет;
- production deployment `dpl_CeoG4DNkr15A4LR4VN7odNB3jTrE` имеет состояние `Ready`, target `production`;
- production отдаёт визуальный банк, модуль каталога, SVG-ресурсы и версию `.8`;
- production browser console и Vercel runtime error logs пусты;
- GitHub Actions `Financial Core emulator`, run `33299490432`, завершён успешно.

Коммит кода: `b65e196` (`feat: add visual worksheet preset library`). Несвязанные пользовательские файлы в `crm-v2/` не добавлялись и не изменялись.

Следующий логичный этап:

1. Versiooniajalugu koos kahe versiooni visuaalse võrdluse ja vana versiooni taastamisega uue versioonina.
2. Õpetaja tulemuste ekraan: vastused plokkide kaupa, kommentaar, vigade kordusülesanne ja seos õppekava oskustega.
3. Kooli enda kinnitatud mallid, mida administraator saab luua olemasolevast heast töölehest.

## Release 30.08.2026.9: современное рабочее пространство конструктора

В CRM v1 обновлён визуальный слой конструктора рабочих листов. Формат данных рабочего листа, Firebase, публичный сайт, CRM v2 и Live Classroom не менялись.

Что изменено:

- верхняя панель получила компактный контекст текущего листа и явный статус `Mustand` / `Salvestatud`;
- вкладки конструктора оформлены как единое рабочее пространство с более ясным активным состоянием;
- панель качества получила визуальную шкалу и понятное итоговое сообщение;
- кнопки добавления блоков, карточки блоков, формы и шаблоны получили единый современный стиль;
- заголовки `Lisa plokk` и `Plokid` отделяют создание от редактирования и объясняют действие;
- предпросмотр оформлен как отдельная A4-зона с закреплённой панелью инструментов;
- добавлен режим `Peida tööriistad`, который скрывает левую панель и освобождает экран для проверки листа;
- на мобильном экране инструменты остаются доступными, горизонтального переполнения страницы нет, полосы служебной прокрутки верхней панели скрыты;
- версия CRM: `KeeleSepp CRM · 30.08.2026.9`.

Изменённые файлы:

- `haldus-worksheet/index.html`;
- `haldus.html`;
- `haldus-shared.js`;
- `worksheet-builder-ui.test.js`;
- `student-export-ui.test.js`.

Проверка релиза:

- `node --test *.test.js`: 198/198 тестов CRM v1 прошли;
- `npm test` в `functions/`: 103/103 серверных теста прошли;
- всего 301/301 проверка;
- `git diff --check` прошёл;
- браузером проверены обычный режим и режим фокусировки на листе;
- мобильная проверка 390x844: ширина документа не превышает viewport, вкладки и инструменты доступны;
- production deployment `dpl_5rPDWf4M1M8iaJbnvxKzqVCbPB6t` имеет состояние `Ready`, target `production`;
- основной домен отдаёт `.9`, новый workspace shell и обновлённый конструктор;
- production smoke-test подтвердил заголовок, статус сохранения, шкалу качества, предпросмотр и кнопку скрытия инструментов;
- Vercel runtime error logs за период публикации пусты;
- GitHub Actions `Financial Core emulator`, run `33302639618`, завершён успешно.

Коммит кода: `cf4f97f` (`feat: modernize worksheet builder workspace`). Несвязанные пользовательские файлы в `crm-v2/` не добавлялись и не изменялись.

Следующий логичный этап конструктора:

1. История версий с визуальным сравнением и восстановлением старой версии как новой.
2. Экран результатов преподавателя: ответы по блокам, комментарий, повторное назначение ошибок и связь с навыками программы.
3. Школьные утверждённые шаблоны, создаваемые администратором из качественного рабочего листа.

## Release 30.08.2026.10: безопасная очистка будущего расписания преподавателя

В недельном календаре CRM v1 добавлено действие `Eemalda minu tunnid alates …`, позволяющее преподавателю быстро освободить собственное расписание начиная с понедельника выбранной недели. CRM v2, публичный сайт и Live Classroom не менялись.

Правила операции:

- действие работает только с расписанием текущего авторизованного преподавателя;
- основная привязка выполняется по `teacherUid`, а совпадение имени используется только для старых записей без UID;
- активная повторяющаяся серия не удаляется: её `endDate` устанавливается на день перед выбранной неделей;
- будущий разовый урок или серия, которая ещё не началась, получает статус `Tühistatud`;
- проведённые уроки, прошлое расписание, счета, платежи и журнал уроков не изменяются;
- групповые уроки обрабатываются теми же безопасными правилами;
- события, импортированные из Google Calendar, не меняются автоматически и показываются отдельным счётчиком;
- перед применением сервер возвращает предварительный расчёт, после которого требуется отдельное явное подтверждение;
- сервер повторно проверяет владельца каждой записи в транзакции, поддерживает идемпотентный `requestId` и сохраняет итог операции в `activityLog`.

Интерфейс:

- кнопка доступна в недельном режиме рядом с фильтрами преподавателей;
- дата в кнопке всегда соответствует понедельнику видимой недели;
- окно предварительного просмотра показывает общее количество, индивидуальные/серийные и групповые записи;
- в окне явно указано, что история, проведённые уроки, счета и платежи сохраняются;
- после успешного применения отменённые записи автоматически скрываются, чтобы расписание было удобно собирать заново.

Изменённые и добавленные файлы:

- `functions/schedule-clear-core.js`;
- `functions/schedule-clear-core.test.js`;
- `functions/index.js`;
- `haldus.html`;
- `haldus-shared.js`;
- `calendar-overlap-ui.test.js`;
- `student-export-ui.test.js`.

Проверка релиза:

- `node --test *.test.js`: 199/199 тестов CRM v1 прошли;
- `npm test` в `functions/`: 106/106 серверных тестов прошли;
- всего 305/305 проверок;
- `git diff --check` и синтаксическая проверка серверного файла прошли;
- Firebase function `staffOperationsApi` успешно опубликована в `us-central1`;
- production deployment `dpl_EV2husTZYXGsJXfq3UpDT6yRfXm7` имеет состояние `Ready`, target `production`;
- основной домен отдаёт версию `KeeleSepp CRM · 30.08.2026.10`;
- production-проверка под реальным администратором подтвердила кнопку для недели с 31.08.2026 и рабочий серверный preview;
- preview показал 39 записей KeeleSepp: 30 индивидуальных/серийных и 9 групповых; 20 импортированных Google-событий корректно оставлены без изменений;
- подтверждение удаления в production не нажималось, поэтому при проверке ни один реальный урок не был изменён;
- GitHub Actions `Financial Core emulator`, run `33332210906`, завершён успешно.

Коммит кода: `271646b` (`feat: safely clear teacher future schedule`). Несвязанные пользовательские файлы в `crm-v2/` не добавлялись и не изменялись.

## Release 30.08.2026.11: предотвращение и безопасное объединение дублей учеников

В CRM v1 переработан весь цикл работы с повторными карточками ученика: обнаружение, предупреждение до создания, ручная проверка и серверное объединение без потери связанных данных. CRM v2, публичный сайт и Live Classroom не менялись.

Почему прежний механизм был недостаточен:

- список находил в основном только полностью одинаковое имя или email;
- короткое имя и полная форма, например `Milan` и `Milan Grozovski`, могли остаться разными карточками;
- регистрация могла сопоставить существующую карточку только по имени, что небезопасно для одноимённых учеников;
- связи уроков, счетов и аккаунтов переносились, но различающиеся поля профиля не были явно собраны в основной карточке и preview.

Новая логика обнаружения:

- сильные признаки: один UID ученического аккаунта, один email или телефон;
- дополнительные признаки: одинаковое имя, короткая/полная форма имени, тот же родитель, преподаватель, предмет и уровень;
- каждая группа получает уровень `Tugev vaste`, `Tõenäoline` или `Kontrolli` и список понятных причин;
- совпадение только по общему родителю не объединяет братьев и сестёр;
- записи одного учебного `groupId` не считаются дублями;
- кириллица и латиница не ломают поиск, но транслитерация автоматически не угадывается;
- объединение всегда запускает администратор вручную после выбора основной карточки и серверного preview.

Защита при создании и регистрации:

- новая карточка проверяется на вероятные совпадения до записи;
- при вероятном совпадении администратор получает явное предупреждение и может продолжить только осознанно;
- такое исключение помечается `duplicateCreationOverride` и сохраняет `potentialDuplicateIds` для дальнейшей проверки;
- регистрация больше не связывает человека с карточкой только по имени;
- допустимы стабильный UID, явно указанный ID карточки или единственный точный email;
- если по точному email найдено несколько карточек, создаётся отдельная запись, помеченная для проверки, вместо случайного выбора;
- родитель при повторном добавлении уже существующего ребёнка переходит к найденной карточке, а новый дубль не создаётся.

Что сохраняется при объединении:

- уроки, расписание, домашние задания, счета и другие документы с `studentId` переводятся на основную карточку;
- сохраняются все ученические и родительские аккаунты и связи групп;
- разные варианты имени, email, телефона, преподавателя и предмета сохраняются как aliases;
- контакты заполняются из дубля только если на основной карточке поле пустое;
- различающиеся уровни, пакеты, контакты и другие поля сохраняются в `mergedProfileSnapshots` и показываются в preview как конфликты;
- исходные карточки не удаляются: они архивируются с `mergedIntoStudentId`;
- операция идемпотентна и записывается в журнал действий.

Production-проверка:

- версия CRM: `KeeleSepp CRM · 30.08.2026.11`;
- экран `Andmebaas` под реальным администратором показал 10 групп на проверку;
- среди видимых вероятных совпадений: `Daniel ↔ Daniel Titov`, `Karolina ↔ Karolina Izovit`, `Yuliia ↔ Yuliia Konovaluyk`, две карточки `Miron`;
- более слабые совпадения (`Anna Krupenya`, `Maria`, `Vladimir Vassiljev`, `Egor`) корректно помечены как `Kontrolli`, а не объединены автоматически;
- в production ни одна реальная карточка не объединялась: проверялись только поиск, объяснения и доступность интерфейса;
- защищённый серверный endpoint без Firebase ID token возвращает `401`, поэтому сторонний пользователь не может выполнить preview или объединение.

Изменённые и добавленные файлы:

- `functions/student-merge-core.js`;
- `functions/student-merge-core.test.js`;
- `functions/index.js`;
- `haldus-shared.js`;
- `haldus.html`;
- `student-duplicate-ui.test.js`;
- `student-export-ui.test.js`.

Проверка релиза:

- `node --test *.test.js`: 203/203 теста CRM v1 прошли;
- `npm test` в `functions/`: 110/110 серверных тестов прошли;
- всего 313/313 проверок;
- `git diff --check` и синтаксическая проверка серверного файла прошли;
- Firebase function `staffOperationsApi` успешно опубликована в `us-central1`;
- production deployment `dpl_3DkekbyiBMVHb251oeBhh17pM8EU` имеет состояние `Ready`, target `production`;
- основной домен и авторизованная CRM отдают версию `.11`;
- GitHub Actions `Financial Core emulator`, run `33333063645`, завершён успешно.

Коммит кода: `db63562` (`feat: prevent and safely merge student duplicates`). Несвязанные пользовательские файлы в `crm-v2/` не добавлялись и не изменялись.

Практический следующий шаг:

1. В `Andmebaas` проверить каждую из 10 групп, выбрать карточку с наиболее полными и актуальными данными как `Põhikaart`.
2. Нажать `Kontrolli ja ühenda`, прочитать серверный preview и выполнить объединение только для подтверждённых дублей.
3. Для одноимённых учеников без общего аккаунта, email или родителя оставить карточки раздельными.
